import React from 'react';
import {Text, View} from 'react-native';

const HelpPage: React.FC = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{color: '#000000'}}>HelpPage</Text>
    </View>
  );
};

export default HelpPage;
